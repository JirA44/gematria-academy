// Configuration Firebase (placeholder)
console.log('🔥 Firebase config chargé (placeholder)');

// Initialisation Firebase sera faite ici
export const firebaseConfig = {
    // Configuration à venir
};
